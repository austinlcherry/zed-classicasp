(function
  .
  (new_identifier (identifier) @name)
) @item

(subroutine
  .
  (new_identifier (identifier) @name)
) @item

(ptrsafe_function_declaration
  .
  (new_identifier (identifier) @name)
) @item
